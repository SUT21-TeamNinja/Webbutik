@model Movie


<div class="col-sm-3">
    <div class="card mb-5" style="width: 13rem; height: 18rem">
        <div class="row g-0" styel="position:relative">
            <div class="col-md-4" style="width: 7rem; height: 10rem;">
                <img src="@Model.ImageUrl" class="img-fluid rounded-start" alt="<IMAGE MISSING>" style="width: 7rem; height: 10rem;">
            </div>
            <div class="col-md-8" style="transform:translateY(0); ">
@*                <div class="card-body" >
*@                  
                    <h6 class="card-title" style="width: 10rem; font-size:small">@Model.Title</h6>
                @await Component.InvokeAsync("DiscountPrice", new { id = Model.Id })
                <a asp-controller="Cart" asp-action="AddToCart" asp-route-id="@Model.Id">
                        <div class="btn btn-success" style="position: absolute;
                             bottom: 0;
                             left: 0;">
                            PURCHASE
                        </div>
                    </a>
@*                </div>
*@            </div>

        </div>
    </div>
</div>