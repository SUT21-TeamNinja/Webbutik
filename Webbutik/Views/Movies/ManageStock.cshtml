
@model IEnumerable<Movie>
@{
    ViewData["Title"] = "ManageStock";
}

<div id="adm-container">
    <div class="wrapper">
  
        <nav id="sidebar">
            <div class="sidebar-header">

                <h3>Admin Page</h3>


            </div>
           
            <ul class="list-unstyled components">
                <p>*Name*</p>
                <li>
                    <a asp-action="ManageProducts">Manage Products</a> 
                </li>
                <li>
                    <a asp-action="ManageStock">Manage Stock</a>
                </li>
                <li>
                    <a asp-action="ManageCampaigns">Manage Campaigns</a>
                </li>
                <li>
                    <a asp-action="Dashboard">Statistic Dashboard</a>
                </li>
                <li>
                    <a asp-action="PurchaseLog" >Customer purchase log</a>
                </li>
                <li>
                    <a asp-action="Currency" >Currency</a>
                </li>
            </ul>
        </nav>
        <div id="content-sidebar"> 

            <h1>Manage Stock</h1>

@*            <table class="table table-responsive-sm">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Title)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Description)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.InStock)
                        </th>
                    </tr>
                </thead>

                <tbody>*@
                    <div class="row row-cols-1 row-cols-md-3 g-4">
                    @foreach (var item in Model)
                    {
@*                        <tr>
                            <td>@item.Title</td>
                            <td>@item.Description</td>
                            <td>@item.InStock</td>
                            <td>
                                

                                <a asp-controller="Movies" asp-action="ManageStockIndividually" asp-route-id="@item.Id">
                                    <i class="btn btn-primary">Manage Stock</i>
                                </a>
                               

                              
                            </td>
                        </tr>*@

                    <div class="card" style="width: 35rem; height:50rem;">
                       
                        <div class="card-body">
                        <img src="@item.ImageUrl" class="img-thumbnail" style="height:30rem;width:20rem;" alt="Movie cover">
                            <h5 class="card-title">@item.Title</h5>
                            <p class="card-text">@item.Description</p>
                                <a asp-controller="Movies" asp-action="ManageStockIndividually" asp-route-id="@item.Id">
                                    <i class="btn btn-primary">Manage Stock</i>
                                </a>
                        </div>
                    </div>
                        
                    }
            </div>

            @*                </tbody>
            </table>*@
          
            



        </div>
    </div>
</div>

